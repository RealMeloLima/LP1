#include <stdio.h>

int main(){
    int array[5];
    int sum = 0;
    printf("Insira 5 números inteiros:\n");
    for(int i = 0; i < 5; i++){
        scanf("%d", &array[i]);
        sum += array[i];
    }
    printf("Sum = %d\n", sum);
    return 0;
}
